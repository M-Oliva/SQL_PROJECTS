/* CSV data */

/*
First,  the information is cleaned in excel, changing the 
data type

Then, the data will be imported in CSV format for each of 
the tables created in the previous resource, as follows:

	1. We go to the tables that are in the chosen schema.

	2. Right click on the table to which data is to be
	added, choose the Import/Export Data option.

	3. We choose import, we search for the file, we choose
	the CSV file type and UTF8 encoding.

	4. Then we go to options, activate Header and the 
	delimiter that our file has.


-- Testing
*/

SELECT * FROM products;

SELECT * FROM stores;

SELECT * FROM sales;

SELECT * FROM inventory;




















